# MINI-SPOTIFY
#FRONTEND<br>basic HTML, CSS and JS based containing sign up, log in, home(playlist), library(favorite) pages
#BACKEND<br>FastAPI containing, register, login, add playlist, add favorite, remove favorite, etc
#DATABASE<br>SQLlite having playlists, songs and users
